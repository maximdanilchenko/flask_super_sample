# flask_super_sample (in development)
Sample Flask project with all basic features for building backend services including ORM (SQLAlchemy), validation (Marshmallow), autodocumentation (apispec), testing (pytest)
